# Release fanout — Step 1 of #191.
#
# Triggered by pushing a `v*` tag. Does:
#
#   1. Build + test (gate — bad tag never publishes)
#   2. Verify the tag matches every package/plugin version and marketplace ref
#   3. Pack every public SDK package
#   4. Publish to npmjs with npm dist-tag inferred from the prerelease
#      suffix (see § "npm tag inference" below)
#   5. Verify npmjs metadata propagated
#   6. Mirror the same package tarballs to GitHub Packages
#   7. Create a GitHub release with auto-generated notes
#   8. Dispatch `aotter/mantle-starters` with the new SDK version
#      so its `bump-from-sdk.yml` workflow opens a downstream bump PR
#
# Steps 2-3 of the fanout (starters bump → tag → landing bump → deploy)
# live in the downstream repos' workflows; see the issue body for the
# whole chain.
#
# ── Operator setup (one-time, repo secrets) ───────────────────────────
#
#   NPM_TOKEN              — npmjs automation token with publish access to
#                            `@aotter/*`. Repo Settings → Secrets →
#                            Actions → New repository secret.
#
#   GitHub Packages publish uses this workflow's GITHUB_TOKEN, so no extra
#   package secret is needed. The packages are mirrors; npmjs remains the
#   canonical public registry for adopters.
#
#   RELEASE_FANOUT_TOKEN   — fine-grained PAT (or GitHub App installation
#                            token) with `contents: write` +
#                            `pull-requests: write` on `mantle-starters`.
#                            Used for the `repository_dispatch` API call.
#                            GITHUB_TOKEN can't dispatch across repos, so
#                            this is required.
#
# Without these secrets, the workflow fails fast at the publish / dispatch
# step — the publish gate already gates on tests, so a missing token can't
# leak a partial release.
#
# ── npm tag inference ────────────────────────────────────────────────
#
#   v1.2.3            → latest
#   v0.0.11-alpha.4   → alpha
#   v0.0.11-beta.1    → beta
#   v0.0.11-rc.1      → rc
#
# The mapping is in the "Extract version + infer npm tag" step. Without
# it, every prerelease would land on `latest` and break adopters running
# `npm install @aotter/mantle` without a tag.

name: release

on:
  push:
    tags:
      - 'v*'

# Only one release run at a time per tag — if two tag pushes race
# somehow, the later one queues until the first finishes.
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write   # create GitHub release
  packages: write   # mirror SDK packages to GitHub Packages
  id-token: write   # npm provenance (when we opt in later)

jobs:
  release:
    runs-on: ubuntu-latest
    # Single source of truth for the five public packages. PKG_DIRS is in
    # dependency order for pack; tarball names derive from PKG_NAMES by
    # stripping `@` and mapping `/` to `-`, matching pnpm pack's naming.
    env:
      PKG_DIRS: >-
        packages/mantle-spec
        packages/mantle-admin-ui
        packages/mantle-runtime
        packages/adapters/cloudflare
        packages/mantle
      PKG_NAMES: >-
        @aotter/mantle-spec
        @aotter/mantle-admin-ui
        @aotter/mantle-runtime
        @aotter/mantle-cloudflare
        @aotter/mantle
    steps:
      - uses: actions/checkout@v7.0.0

      - uses: pnpm/action-setup@v6.0.9

      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 22
          cache: pnpm
          registry-url: 'https://registry.npmjs.org'

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build (release gate)
        run: pnpm build

      - name: Test (release gate)
        run: pnpm test

      - name: Extract version + infer npm tag
        id: ver
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          case "$VERSION" in
            *-alpha*) NPM_TAG=alpha ;;
            *-beta*)  NPM_TAG=beta ;;
            *-rc*)    NPM_TAG=rc ;;
            *)        NPM_TAG=latest ;;
          esac
          echo "npm_tag=$NPM_TAG" >> "$GITHUB_OUTPUT"
          echo "Tag $TAG → version $VERSION → npm dist-tag $NPM_TAG"

      - name: Verify package, plugin, and marketplace versions match ${{ steps.ver.outputs.version }}
        run: |
          set -euo pipefail
          node --input-type=module -e "
            import { readdirSync, readFileSync } from 'node:fs';
            const expected = '${{ steps.ver.outputs.version }}';
            let failed = false;
            const packagePaths = [
              'package.json',
              ...readdirSync('packages', { recursive: true })
                .filter((path) => path.endsWith('/package.json') && !path.includes('/node_modules/'))
                .map((path) => 'packages/' + path),
            ];
            for (const path of packagePaths) {
              const pkg = JSON.parse(readFileSync(path, 'utf8'));
              if (pkg.version !== expected) {
                console.error(path + ' version ' + pkg.version + ' does not match tag version ' + expected);
                failed = true;
              }
            }
            for (const path of [
              '.codex-plugin/plugin.json',
              '.claude-plugin/plugin.json',
              '.copilot-plugin/plugin.json',
              '.cursor-plugin/plugin.json',
            ]) {
              const plugin = JSON.parse(readFileSync(path, 'utf8'));
              if (plugin.version !== expected) {
                console.error(path + ' version ' + plugin.version + ' does not match tag version ' + expected);
                failed = true;
              }
            }
            const marketplacePath = '.agents/plugins/marketplace.json';
            const marketplace = JSON.parse(readFileSync(marketplacePath, 'utf8'));
            const marketplaceRef = marketplace.plugins?.[0]?.source?.ref;
            const expectedRef = 'v' + expected;
            if (marketplaceRef !== expectedRef) {
              console.error(marketplacePath + ' ref ' + marketplaceRef + ' does not match tag ' + expectedRef);
              failed = true;
            }
            if (failed) process.exit(1);
          "

      - name: Pack release tarballs
        run: |
          set -euo pipefail
          VERSION="${{ steps.ver.outputs.version }}"
          mkdir -p .release-tarballs
          for dir in $PKG_DIRS; do
            pnpm -C "$dir" pack --pack-destination "$PWD/.release-tarballs"
          done

          # Version lockstep: pnpm pack names tarballs <name>-<version>.tgz,
          # so a package whose version drifted from the tag fails here.
          for pkg in $PKG_NAMES; do
            base="${pkg#@}"
            test -f ".release-tarballs/${base//\//-}-$VERSION.tgz"
          done

      - name: Publish to npmjs
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${NODE_AUTH_TOKEN:-}" ]; then
            echo "::error::NPM_TOKEN is not set; refusing to start a partial release."
            exit 1
          fi
          NPMJS_NPMRC="$PWD/.npmrc.npmjs"
          {
            echo "registry=https://registry.npmjs.org/"
            echo "//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}"
            echo "always-auth=true"
          } > "$NPMJS_NPMRC"
          npm whoami --userconfig "$NPMJS_NPMRC" --registry https://registry.npmjs.org >/dev/null
          VERSION="${{ steps.ver.outputs.version }}"
          PUBLISH_TAG="${{ steps.ver.outputs.npm_tag }}"
          # Pre-v0.1 policy: npm latest follows the most user-useful
          # prerelease. Publishing with the primary tag set to latest
          # also keeps npmjs.com's package README metadata fresh.
          if [[ "$VERSION" == 0.0.* && "$PUBLISH_TAG" != "latest" ]]; then
            PUBLISH_TAG=latest
          fi

          for pkg in $PKG_NAMES; do
            base="${pkg#@}"
            tgz=".release-tarballs/${base//\//-}-$VERSION.tgz"
            if npm view "$pkg@$VERSION" version \
              --userconfig "$NPMJS_NPMRC" \
              --registry https://registry.npmjs.org >/dev/null 2>&1; then
              echo "$pkg@$VERSION already exists on npmjs; skipping publish"
              continue
            fi
            npm publish "$tgz" \
              --userconfig "$NPMJS_NPMRC" \
              --registry https://registry.npmjs.org \
              --tag "$PUBLISH_TAG" \
              --access public
          done

          if [[ "$PUBLISH_TAG" != "${{ steps.ver.outputs.npm_tag }}" ]]; then
            for p in $PKG_NAMES; do
              npm dist-tag add "$p@$VERSION" "${{ steps.ver.outputs.npm_tag }}" \
                --userconfig "$NPMJS_NPMRC" \
                --registry https://registry.npmjs.org
            done
          fi

      - name: Verify npm metadata
        run: |
          set -euo pipefail
          VERSION="${{ steps.ver.outputs.version }}"
          for attempt in 1 2 3 4 5 6 7 8; do
            READY=1
            for p in $PKG_NAMES; do
              npm view "$p@$VERSION" version readmeFilename --json --registry https://registry.npmjs.org || READY=0
            done
            README_LEN=$((npm view "@aotter/mantle@$VERSION" readme --json --registry https://registry.npmjs.org 2>/dev/null || true) | node -e "let s=''; process.stdin.on('data',d=>s+=d); process.stdin.on('end',()=>{const v=s.trim()?JSON.parse(s):''; console.log((v || '').length);})")
            if [ "$READY" = 1 ] && [ "$README_LEN" -ge 200 ]; then
              exit 0
            fi
            echo "npm metadata not ready yet (attempt $attempt, mantle readme length $README_LEN); retrying"
            sleep 15
          done
          echo "::error::npm metadata did not become ready or @aotter/mantle@$VERSION readme stayed empty"
          exit 1

      - name: Mirror to GitHub Packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          GPR_NPMRC="$PWD/.npmrc.github"
          {
            echo "@aotter:registry=https://npm.pkg.github.com"
            echo "//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}"
            echo "always-auth=true"
          } > "$GPR_NPMRC"
          VERSION="${{ steps.ver.outputs.version }}"
          PUBLISH_TAG="${{ steps.ver.outputs.npm_tag }}"
          if [[ "$VERSION" == 0.0.* && "$PUBLISH_TAG" != "latest" ]]; then
            PUBLISH_TAG=latest
          fi
          mkdir -p .github-package-tarballs

          for pkg in $PKG_NAMES; do
            base="${pkg#@}"
            tgz=".release-tarballs/${base//\//-}-$VERSION.tgz"
            if npm view "$pkg@$VERSION" version \
              --userconfig "$GPR_NPMRC" \
              --registry https://npm.pkg.github.com >/dev/null 2>&1; then
              echo "$pkg@$VERSION already exists on GitHub Packages; skipping publish"
              continue
            fi
            work="$(mktemp -d)"
            tar -xzf "$tgz" -C "$work"
            node -e "const fs=require('node:fs'); const path=process.argv[1]; const pkg=JSON.parse(fs.readFileSync(path,'utf8')); pkg.publishConfig={...(pkg.publishConfig||{}), registry:'https://npm.pkg.github.com'}; fs.writeFileSync(path, JSON.stringify(pkg,null,2)+'\n');" "$work/package/package.json"
            (
              cd "$work/package"
              npm pack --pack-destination "$GITHUB_WORKSPACE/.github-package-tarballs" >/dev/null
            )
            gpr_tgz="$GITHUB_WORKSPACE/.github-package-tarballs/$(basename "$tgz")"
            npm publish "$gpr_tgz" \
              --userconfig "$GPR_NPMRC" \
              --registry https://npm.pkg.github.com \
              --tag "$PUBLISH_TAG" \
              --access public
            rm -rf "$work"
          done

          if [[ "$PUBLISH_TAG" != "${{ steps.ver.outputs.npm_tag }}" ]]; then
            for p in $PKG_NAMES; do
              npm dist-tag add "$p@$VERSION" "${{ steps.ver.outputs.npm_tag }}" \
                --userconfig "$GPR_NPMRC" \
                --registry https://npm.pkg.github.com
            done
          fi

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          # Mark prereleases so GitHub UI / consumers can filter.
          PRERELEASE_FLAG=""
          if [[ "${{ steps.ver.outputs.npm_tag }}" != "latest" ]]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          if gh release view "${{ github.ref_name }}" >/dev/null 2>&1; then
            echo "GitHub release ${{ github.ref_name }} already exists; skipping create."
          else
            gh release create "${{ github.ref_name }}" \
              --generate-notes \
              --title "${{ github.ref_name }}" \
              $PRERELEASE_FLAG
          fi

      - name: Dispatch starters bump-from-sdk
        env:
          GH_TOKEN: ${{ secrets.RELEASE_FANOUT_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::warning::RELEASE_FANOUT_TOKEN not set — starters bump skipped. SDK is published to npmjs and GitHub Packages; set the secret and re-dispatch via the Actions UI to continue the fanout."
            exit 0
          fi
          gh api "repos/${{ github.repository_owner }}/mantle-starters/dispatches" \
            -f event_type=sdk-published \
            -F "client_payload[sdk_version]=${{ steps.ver.outputs.version }}" \
            -F "client_payload[npm_tag]=${{ steps.ver.outputs.npm_tag }}"
          echo "Dispatched mantle-starters bump-from-sdk for ${{ steps.ver.outputs.version }}"
